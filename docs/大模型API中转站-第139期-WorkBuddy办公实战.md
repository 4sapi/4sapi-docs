---
title: "【大模型API中转站】第139期 WorkBuddy办公实战 | 汇报PPT数据一条龙"
category: 人工智能
tags:
  - 大模型API中转站
  - WorkBuddy
  - 日常办公
  - PPT
  - 数据分析
  - 文档处理
  - 深度研究
  - 企业级大模型接入
  - 4SAPI
description: "围绕 WorkBuddy 日常办公场景，拆解文档处理、数据分析可视化、深度研究、幻灯片生成四类高频任务。文章给出从上传资料、优化提示词、Plan确认、Craft执行到产物验收的完整流程，适合第一次把 AI Agent 用进真实办公的人。"
---

# 【大模型API中转站】第139期 WorkBuddy办公实战 | 汇报PPT数据一条龙

本文是【大模型API中转站】系列的第139篇。本系列致力于用最低的成本、最清晰的方法，帮你打通多模型API的任督二脉。建议先收藏，随用随查。

上一期讲 WorkBuddy 怎么安装、登录、看主界面。

这一期直接进入办公场景。

先说结论：

```text
WorkBuddy 最适合普通人上手的地方，不是写代码。
而是日常办公。
```

因为大多数人的真实工作，不是每天重构项目。

而是：

```text
写汇报。
整理资料。
做 PPT。
分析表格。
总结会议。
输出方案。
把一堆零散信息变成能交付的文件。
```

这些事很烦。

但非常适合 Agent。

WorkBuddy 的优势在于，它把这些场景做成了入口和模板。

你不用从空白对话框开始猜提示词。

直接从：

```text
日常办公
-> 文档处理 / 数据分析可视化 / 深度研究 / 幻灯片
```

进去，就能跑一个相对完整的办公任务。

## 1. 先准备工作空间

开始前先建一个工作空间。

例如：

```text
2026-07-部门月报/
2026-07-Skill教程资料/
2026-07-客户方案/
2026-07-竞品分析/
```

不要把所有任务都丢进默认目录。

办公任务最容易出现的问题是：

```text
素材在一个地方。
草稿在一个地方。
最终稿在另一个地方。
PPT 又不知道存哪了。
```

所以第一步就要让 WorkBuddy 有一个明确工作现场。

工作空间里建议放：

```text
source/：原始资料
draft/：草稿
output/：最终产物
logs/：过程记录
```

你可以先不用这么正式。

但至少要保证：

```text
每个任务一个文件夹。
```

## 2. 选日常办公场景

进入 WorkBuddy 后，切到：

```text
日常办公
```

下面通常会看到几类入口：

```text
文档处理
数据分析可视化
深度研究
幻灯片
```

不要小看这些入口。

它们背后往往已经预置了提示词模板。

比如你点：

```text
日常办公 -> 文档处理
```

可能会看到一组模板：

```text
总结文档。
提炼要点。
生成汇报。
改写风格。
提取行动项。
```

这些模板对新手很友好。

你只需要把里面的字段换成自己的真实需求。

不要一开始就写：

```text
帮我整理一下。
```

这句话太空。

要写成：

```text
请阅读我上传的 3 份资料，整理成一份面向领导汇报的月度总结。
要求包含：核心进展、关键数据、主要问题、下月计划。
输出 Markdown，并额外给一个 PPT 大纲。
```

任务越具体，产物越能用。

## 3. 用 Plan 模式先看方案

办公任务虽然看起来低风险，但也建议先用 Plan。

尤其是第一次让它处理你的真实材料时。

流程：

```text
选择工作空间
-> 上传资料
-> 切日常办公
-> 选文档处理
-> 模式选 Plan
-> 输入需求
-> 看计划
-> 确认后 Craft 执行
```

为什么先 Plan？

因为你能提前看到：

```text
它准备读哪些文件。
它准备生成什么。
它会不会误解你的目标。
它有没有遗漏关键输出。
```

计划不对，就先改。

不要等它生成一大堆文件后再返工。

## 4. 新手一定要用“优化提示词”

附件里提到一个很重要的按钮：

```text
优化提示词
```

新手强烈建议点。

原因很简单：

```text
你随口写的需求，通常不够结构化。
```

比如你写：

```text
帮我整理一下 SKILL 相关文章。
```

优化后应该变成类似：

```text
目标：
整理当前工作空间内与 SKILL 相关的文章，形成一份教程选题资料包。

输入：
读取当前工作空间里的 Markdown、Word、PDF 和网页摘录。

输出：
1. 文章主题分类。
2. 每类代表文章。
3. 可写成教程的 10 个选题。
4. 每个选题的目标读者、核心问题和建议标题。
5. 最后生成一份 Markdown 报告。

限制：
不要删除或移动原始文件。
不确定的地方标注“待确认”。
```

这就是普通提示词和结构化提示词的差别。

对 Agent 来说，结构化提示词更容易执行。

尤其是国产模型，有时你把目标、输入、输出、限制写清楚，效果会立刻稳定很多。

## 5. 案例一：把资料整理成汇报稿

适合场景：

```text
周报。
月报。
项目复盘。
领导汇报。
客户沟通材料。
```

准备材料：

```text
会议纪要。
项目进展。
数据截图。
旧文档。
聊天记录摘录。
```

提示词模板：

```text
请阅读当前工作空间 source/ 里的资料。

目标：
整理成一份面向领导的项目进展汇报。

输出：
1. 标题。
2. 300 字以内摘要。
3. 本阶段完成的 5 个关键进展。
4. 目前存在的 3 个问题。
5. 下阶段计划。
6. 需要领导决策或协调的事项。

要求：
- 语气正式但不要官话太重。
- 所有具体数据必须标注来源文件。
- 不确定的信息写“待确认”。
- 输出保存为 output/project-report.md。

开始前先列处理计划，不要直接写最终稿。
```

验收时看五点：

```text
有没有把背景讲清楚。
有没有把进展和问题分开。
有没有凭空编数字。
有没有行动项。
有没有适合直接发给领导的语气。
```

## 6. 案例二：把材料变成 PPT 大纲

WorkBuddy 里有幻灯片入口。

但我建议第一步不要直接让它生成完整 PPT。

先让它生成：

```text
PPT 大纲。
```

提示词：

```text
请基于当前工作空间的资料，生成一份 10 页 PPT 大纲。

主题：
WorkBuddy 在日常办公中的应用方案。

受众：
没有技术背景的部门同事。

页面结构：
1. 封面
2. 当前办公痛点
3. WorkBuddy 能解决什么
4. 文档处理示例
5. 数据分析示例
6. PPT 生成示例
7. 使用流程
8. 风险和注意事项
9. 一周试点计划
10. 总结

每页输出：
- 标题
- 3-5 个要点
- 建议配图或图表
- 演讲备注

不要直接生成 PPT 文件，先给大纲。
```

大纲确认后，再说：

```text
按这个大纲生成 PPT。
风格简洁、商务、适合内部汇报。
如果需要图表，请先用占位说明，不要编造数据。
```

这样比一上来就“帮我做个 PPT”稳很多。

## 7. 案例三：数据分析和可视化

WorkBuddy 处理表格时，最重要的是别只让它“分析一下”。

要指定问题。

例如：

```text
请读取我上传的 Excel。

目标：
分析最近 3 个月销售数据变化。

请输出：
1. 总体趋势。
2. 增长最快的 5 个产品。
3. 下滑最明显的 5 个产品。
4. 异常数据点。
5. 建议做成哪些图表。
6. 一段适合放进汇报 PPT 的结论。

要求：
- 不要修改原始表格。
- 计算过程写清楚。
- 如果字段含义不明确，先问我。
```

数据任务最怕两件事：

```text
字段含义理解错。
模型凭感觉下结论。
```

所以要让它先输出：

```text
字段解释。
计算逻辑。
异常值。
```

再让它做图表。

## 8. 案例四：深度研究

深度研究适合这类任务：

```text
竞品分析。
行业趋势。
工具选型。
政策梳理。
技术路线对比。
```

如果 WorkBuddy 可以联网或调用搜索类技能，就能把资料收集和报告生成串起来。

提示词：

```text
请帮我做一份 WorkBuddy 与 Claude Code、Codex 的使用场景对比。

目标读者：
想使用 AI Agent 但不会写代码的普通办公用户。

请输出：
1. 三者定位。
2. 适合人群。
3. 上手门槛。
4. 办公任务能力。
5. 开发任务能力。
6. 权限和风险。
7. 推荐选择。

要求：
- 不要做夸张营销。
- 明确哪些是我提供的材料，哪些是你的推断。
- 给出一张 Markdown 对比表。
- 最后给 5 条适合新手的建议。
```

研究类任务要特别加一句：

```text
不确定就写不确定。
```

否则模型很容易把听起来合理的内容写成事实。

## 9. 产物在哪里看

WorkBuddy 跑完后，通常右侧栏会显示：

```text
Todo 进度
生成文件
预览
工作空间目录
```

你可以直接点预览。

也可以打开本地工作空间目录。

建议养成一个习惯：

```text
每次任务结束，都让它写一份 task-log.md。
```

内容包括：

```text
读取了哪些文件。
生成了哪些文件。
哪些内容需要人工确认。
哪些文件没有动。
```

提示词：

```text
任务结束后，请在 logs/ 下生成一份 task-log.md。
列出：
1. 本次读取的文件。
2. 本次生成的文件。
3. 本次修改的文件。
4. 需要我人工确认的事项。
5. 你没有执行的高风险动作。
```

这个习惯很值钱。

以后你会知道每次 AI 到底干了什么。

## 10. 办公任务验收清单

不要看到格式漂亮就直接交。

办公产物至少检查这几项：

```text
[ ] 标题是否准确。
[ ] 关键事实是否来自原始材料。
[ ] 数据有没有来源。
[ ] 有没有把推断写成事实。
[ ] 语气是否适合受众。
[ ] 是否有可执行行动项。
[ ] 文件是否保存在正确工作空间。
[ ] 是否生成了任务日志。
```

如果是 PPT，再加：

```text
[ ] 每页信息量是否过多。
[ ] 是否缺少故事线。
[ ] 图表是否有数据来源。
[ ] 是否有演讲备注。
```

如果是数据分析，再加：

```text
[ ] 字段含义是否正确。
[ ] 计算口径是否写清楚。
[ ] 异常值是否单独标注。
```

## 11. 和 4SAPI 的关系

个人办公用 WorkBuddy，内置模型就够了。

但团队要长期用，最好把模型调用纳入统一治理。

原因是办公任务很容易批量化：

```text
每天整理会议纪要。
每周生成部门周报。
每月做经营分析。
每天跑竞品研究。
每个项目自动生成 PPT 大纲。
```

这些任务一多，就要看：

```text
谁在用。
用了哪个模型。
花了多少钱。
是否处理了敏感文件。
是否生成了可审计日志。
```

4SAPI 这类企业 API 网关可以放在模型入口层，统一做：

```text
Key 管理。
模型路由。
预算控制。
调用日志。
失败告警。
项目维度统计。
```

新手不一定需要。

团队一定会需要。

## 12. 总结

WorkBuddy 的办公价值，不是“会聊天”。

而是它能把办公任务拆成：

```text
读资料
列计划
生成产物
保存文件
记录过程
```

这比普通聊天机器人实用得多。

第一次用它做办公任务，记住这个流程：

```text
建工作空间。
上传资料。
切日常办公。
用 Plan 看计划。
点优化提示词。
确认后 Craft 执行。
最后按清单验收。
```

只要这套跑顺，汇报、PPT、数据、研究，日常七成烦活基本都能开始交给它。

